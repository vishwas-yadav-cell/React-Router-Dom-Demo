import React from 'react';

const Home = ({ name }) => {
    return (
        <div>
            <h1>This is home page...</h1>
            <h3>Welcome {name}</h3>
        </div>
    );
}

export default Home;