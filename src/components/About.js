import React from 'react';

const About = ({ cName }) => {
    return (
        <div>
            <h1>this is about page...</h1>
            <h3>We are {cName}</h3>
        </div>
    );
}

export default About;